@using Microsoft.AspNetCore.Identity
@inject UserManager<GamblersModel> _userManager

@{
    ViewData["Title"] = "Home Page";
}

<h1>Welcome</h1>

@if(User.Identity.IsAuthenticated)
{
        <p>Welcome, @_userManager.GetUserName(User)</p>
}